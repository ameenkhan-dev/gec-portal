{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "src": "^/api/(.*)",
      "destination": "https://gec-portal-api.onrender.com/api/$1"
    },
    {
      "src": "^(?!api).*",
      "dest": "/index.html"
    }
  ]
}
